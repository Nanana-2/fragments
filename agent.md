<!-- File: agent.md -->
# Fragments | プロジェクト仕様書 & エージェント指示書

本ドキュメントは、SNS（X/Twitter）のフォーマットを模倣したパブリックドメイン文学フィード「**Fragments**」の設計意図、仕様、実装経緯、および今後のタスクを定義した引き継ぎ用ドキュメントである。

---

## 1. プロジェクト概要

- **名称**: Fragments（フラグメンツ）
- **コンセプト**: 
  - SNS特有のレスバ・政治・広告・アルゴリズム中毒に疲弊した活字中毒者に向けて、**「誰とも繋がらず、怒りも起きない無害な言葉の濁流」**を親指1本でカジュアルに消費させるWebアプリケーション。
  - 中核の欲求は「読書」ではなく、**文字を絶えず追い続けたいが、悪口や怒りは浴びたくない**というもの。コピーでは「静けさ」だけでなく「文字の濁流」「予測不能な落差」を前面に出す。
  - 「名言集」「文学鑑賞」のような仰々しさを排し、日常のタイムラインに溶け込むファストな活字体験を提供する。
- **ターゲット**: Xのタイムラインを無目的にリロード・スワイプしてしまう層、短文活字を浴びたい層。

---

## 2. デザイン決定プロセスと脱・AI化の論点

初期プロトタイプから議論を重ね、AIが生成しがちなWebテンプレ表現（AI臭さ）を徹底的に排除した。

| 要素 | 排除されたAI的テンプレ | 採用された設計 | 理由 |
| :--- | :--- | :--- | :--- |
| **表示形式** | 1画面1フレーズのスナップUI | 縦スクロールの連続タイムライン | スナップ式は言葉が重く読書を強いる。親指のスピードで流し読みできる密度が必要。 |
| **フォント** | 雰囲気重視の明朝体 | ネイティブサンセリフ（Noto Sans JP） | 短文のファストな消費において明朝体は視覚的ノイズになる。情報伝達速度を最優先。 |
| **背景色** | わざとらしいクリーム色/羊皮紙色 | 純白（#FFFFFF） / 純黒（#000000） | 「紙の温もり」的な演出を捨て、SNSアプリとしての無機質な画面を作る。 |
| **タイポグラフィ**| 補足情報の文字色を薄くする（グレー化）| 全テキスト同一インク（同一色） | 級数（文字サイズ）と配置のみで階層を表現。Webツール感を抑える。 |
| **コンテンツ** | 教科書の冒頭（方丈記、山羊の歌等） | 随筆、書簡、短詩、愚痴の断片 | 有名すぎるフレーズは「お勉強感」が出るため、引力のある生々しい断片を採用。 |

---

## 3. 法的リスクの整理

1. **UIレイアウト**:
   - 左アイコン、右上に名前/ID/日付、下に本文とアクションボタンという配置は**業界のデファクトスタンダード（アイデア）**であり、著作権侵害には当たらない（CSS/JSは独自実装）。
2. **商標・ロゴの回避**:
   - 「X」「Twitter」「ツイート」「ポスト」「青い鳥」などの商標・アイコンは一切使用しない。
3. **著作権・肖像権のクリア基準**:
   - 1967年以前に没した作家（旧著作権法適用により没後50年で権利消滅）のみを採用。
   - 肖像写真についても1956年以前に撮影・公表されたパブリックドメイン素材に限定。

---

## 4. 技術アーキテクチャ

- **フロントエンド**: 単一の `index.html`（HTML5 / Vanilla CSS / Vanilla JS）。
  - ビルドツール不要。外部ライブラリ依存ゼロ。
  - PWA（Progressive Web App）対応。
  - 安全なストレージラッパー（`storage` オブジェクト）により、`file://` 環境でも例外停止しない堅牢な設計。
- **データ管理**: `quotes.json`（外部静的JSON）。
  - `fetch` で非同期ロード。未配置時はインラインの `fallbackData` に自動フォールバック。
- **データ抽出パイプライン**: `extract_quotes.py`（Python 3）。
  - 青空文庫形式のテキスト（ルビ記法・注記タグ・底本情報）を正規表現で自動クレンジング。
  - 長編小説は抽出対象外とし、短歌・自由律俳句・箴言・身辺随筆・日記のみを明示的な許可リストから抽出する。
  - 12〜100文字、1〜3行、単体で意味が閉じる断片だけをJSONへ書き出す。

### 主要機能の実装仕様
1. **重み付きおすすめアルゴリズム**:
   - `IntersectionObserver` により、画面内に1.5秒以上とどまったポストの作家スコア（`authorAffinities`）を加算。
   - 「いいね」タップ時はスコアをボーナス加算（+3）。
   - 次に表示される作家を先に選び、その作家の候補から一篇を選ぶ。収録件数の多い作家だけが有利にならないようにする。
   - 日常（white）35%、毒・冷笑（black）20%、超越（blue）15%、詩的断片（gold）30%を基準にし、直近と同じ系統は出にくくして落差を作る。
2. **ローカルブックマーク（いいね）**:
   - 他者と繋がるソーシャル機能ではなく、完全に `localStorage` 完結。
   - 数値はカウントアップせず、赤く点灯するのみ。「保存済み」タブで一覧表示。
3. **共有機能**:
   - Web Share API（`navigator.share`）が使えるモバイル端末ではOS標準のシェアシートを呼び出し。
   - 非対応ブラウザではXのポスト画面（Web Intent）を新規タブで起動。
4. **Pull to Refresh（引っ張って更新）**:
  - タッチイベント（`touchstart`, `touchmove`, `touchend`）で上部インジケータを制御し、ネイティブアプリ同様の操作感を実現。
  - ヘッダーの `Fragments` を押しても、通知文を出さずタイムラインを即時更新する。
  - 選択中の「タイムライン」タブをもう一度押した場合も、通知文を出さず即時更新する。
5. **作者アイコン**:
  - Wikimedia CommonsでPublic domainを確認した作者肖像をローカル配信する。
  - 肖像を権利確認できない作者だけ文字アイコンへフォールバックする。

---

## 5. リポジトリ構成

<pre><code>fragments/
├── index.html          # Webアプリケーション本体（本番配信用）
├── quotes.json         # 抽出済みデータ（本番配信用）
├── extract_quotes.py   # データ生成用Pythonスクリプト
├── agent.md            # 本仕様書
└── texts/              # 青空文庫テキスト格納ディレクトリ（開発用）
    ├── 太宰治_人間失格_1948年.txt
    └── 芥川龍之介_河童_1927年.txt
</code></pre>

---

## 6. 今後のタスク・エージェント（Codex）への指示

Codex等のコーディングエージェントは、以下の優先順位に従って作業を進めること。

### Task 1: データの量産パイプラインの実行
- `texts/` フォルダに青空文庫の短歌、自由律俳句、箴言、身辺随筆、日記を収集する。
- 最優先は啄木『一握の砂』『悲しき玩具』、尾崎放哉選句集、山頭火『草木塔』『行乞記』、芥川『侏儒の言葉』。
- `python3 extract_quotes.py` を実行し、現在は20名・800件規模の `quotes.json` を生成する。
- 海外古典は既存の日本語訳を無断転載せず、権利切れ原典・英訳を底本にした独自訳で収録する。

### Task 2: PWA用アセットの静的化
- 現在動的Blobで生成しているマニフェストを独立した `manifest.json` に分離する。
- ホーム画面用アプリアイコン（192x192, 512x512 のPNG画像）および `ogp.png`（1200x630）を作成・配置する。

### Task 3: デプロイ（GitHub Pages / Cloudflare Pages）
- リポジトリをGitHubにプッシュし、Cloudflare PagesまたはGitHub Pagesに接続。
- 静的サイトとしてHTTPSで全世界に公開する。

### Task 4: コンテンツ編集パイプライン
- 長編小説の機械分割は行わない。
- 指示語・代名詞で始まる文、会話の切れ端、読点・接続助詞で終わる文、特殊文字が残る文、平仮名率が極端に低い文を除外する。
- 文字数だけで切らず、文末・括弧・引用符が閉じており、単体で意味が通る断片だけを採用する。
- 自動抽出結果は公開候補であり、将来は `draft → reviewed → published` の編集状態を持たせる。
- 小説偏重を避け、格言・随筆・日記・書簡・東洋古典・権利切れ翻訳を追加する。翻訳作品は原作者だけでなく翻訳者の権利も確認する。
- 詳細な配合、広告、アプリ化、公開基盤の方針は `CONTENT_STRATEGY.md` を参照する。
